# TEST_SPEC.md — fishing-port-atlas-ai

<!-- scaffold template v1.30.0 から展開(2026-09-08) -->

## 実行規約

- `pytest -x -q`(パイプライン・データ)と `npm test`(フロントエンド)を stage 3–5 の判定に使用
- マーカー: `unit` / `integration` / `validation`
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提を**テスト内の assert で検算**する

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。SPEC の保証粒度を超える期待値を書かない |
| 外部権威(公表値) | 出典 URL と取得日をフィクスチャの先頭に書く |
| 実測 | 実測日と実測値をコメントに残す |

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| `data/normalized/jfa_official_counts.json` | 水産庁 総括表 `sub81-256.pdf` / 都道府県別 `sub81-257.pdf` | **非循環**。明細 PDF(2,768 行)とは別の文書。明細を数えた値を明細の検査に使わない |
| 総括表脚注の特定第３種 13 港名 | 同上 | **非循環**。種別欄とは独立に、港名の集合で照合できる |
| 都道府県別表の管理者内訳(都道府県/市町村) | `sub81-257.pdf` | **非循環**。明細の管理者名欄から導いた区分と突き合わせる |
| C09 の市区町村コード | 国土数値情報 C09-06 | 都道府県コード定数表(`data-pipeline/common/prefectures.py`)の検算に使う |
| `tests/fixtures/pdf_geometry.json` | 秋田県 `sub81-262.pdf` の罫線座標(実測 2026-09-08) | 取込器の幾何仮定を固定する |
| 気象庁 `eg_info.html` の記載 | 「大阪湾・播磨灘・備讃瀬戸・備後灘・燧灘・安芸灘・伊予灘・周防灘は速報のみで平年値なし」 | **非循環**。名前の一覧は文章から、平年値の有無はデータファイルから独立に来る |
| `lib/ocean.ts` の `olsTrend` | Python `build_ocean.py` の独立実装 | **二実装照合**。結論(傾き)だけでなく中間量(Sxx / Sxy / 残差平方和)も比べる |

**循環していないことの確認**: 件数オラクル(総括表・都道府県別)は明細 PDF を一度も読まずに
作られる。取込器を壊しても、この 2 枚は動かない。

## ケース一覧

**「対応要求」には SPEC の ID を必ず書く**(HC-157)。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-01 / G-01 | canonical の港数 | 総括表の総計と一致 |
| T-002 | F-01 / G-02 | 種別内訳 | 総括表の種別内訳と一致(1/2/3/特3/4) |
| T-003 | F-01 / G-03 | 都道府県別の港数 | 都道府県別表の 40 行すべてと一致 |
| T-004 | F-01 / G-03 | 都道府県 × 種別 | 都道府県別表の「第３種」は 3 + 特3 として一致 |
| T-005 | F-01 / G-03 | 都道府県 × 管理者区分 | 明細の管理者名から導いた区分が都道府県別表と一致 |
| T-006 | F-01 / G-04 | 特定第３種の集合 | 総括表脚注の 13 港名と集合として一致 |
| T-007 | F-01 / G-05 | 漁港番号の一意性 | 重複 0 件 |
| T-008 | F-02 / G-06 | 座標充足率 | 90% 以上(実測 98.30%) |
| T-009 | F-02 / G-07 | 座標の範囲 | 全件が緯度 20–46 / 経度 122–154 |
| T-010 | F-01 | 集計表そのものの内部整合 | 種別内訳の和・管理者内訳の和が各行の総計と一致 |
| T-011 | F-01 | **陽性対照**: 取込器の幾何仮定を崩す | 列数・横罫が想定と違う入力で `ParseError` が上がる |
| T-012 | F-01 | **陽性対照**: 件数照合が壊れた入力を落とす | 1 港削った入力で照合が失敗する |
| T-013 | F-02 / G-06 | **陽性対照**: 外接矩形ガード | 県の範囲外にある区域線重心を採用しない(石川県 2410125 が実例) |
| T-014 | F-01 | 都道府県コード表の検算 | C09 の市区町村コードとの多数決に一致し、30 県以上で検算できている |
| T-015 | F-10 / G-08 | 欠損の表現 | 詳細 JSON に `0` で埋めた欄が無く、`status` 語彙のみを使う |
| T-016 | F-03 | 検索 | 港名・読み・都道府県・市町村のいずれでも引ける |
| T-017 | F-03 | 検索の正規化 | 全角/半角・カナの揺れを吸収する |
| T-018 | F-02 | GeoJSON 化 | 座標なしの港は feature を作らない(0,0 に置かない) |
| T-019 | F-03 | 種別フィルタ | 選択した種別だけが残り、空選択は全件 |
| T-020 | N-02 | API キー混入 | ビルド成果物に秘密の名前が現れない |
| T-021 | G-09 | 字種検査 | `harness/text_hygiene.py` が違反 0 件 |
| T-022 | F-09 | 出典の到達性 | すべての港の詳細 JSON が 1 件以上の `sources` を持つ |
| T-023 | F-06 / F-04 | 実ブラウザ検品 | 地図に点が描かれ、クリックで Drawer が開く(`npm run verify:browser`) |
| T-024 | F-05 / G-12 | 二実装照合(傾き) | Python の傾きを TS が全 107 海域で再現し、**中間量**(標準誤差・t・R²)も一致 |
| T-024 | F-05 / G-12 | **陽性対照** | 系列を 1 年ずらした実装は照合に落ちる |
| T-025 | F-05 / G-14 | 系列の長さ | 長期 102 は 1982 年始まり、短期 5 は 2016 年始まりで平年値なし |
| T-026 | F-05 / G-15 | 図の収まり | スパークラインの全点が viewBox 内。p 値を 0 と書かない |
| T-027 | F-05 | 説明文書との突合 | 気象庁が名指しした 5 海域 = 平年値を持たない 5 海域(**非循環**) |
| T-028 | F-05 | 解析開始年 | 長期系列はすべて 1982 年始まり |
| T-029 | F-05 | 部分年の除外 | 取得時点の当年が回帰に入っていない |
| T-030 | F-05 | 平年値の期間 | ファイルの記載から読めており、全海域で同一 |
| T-031 | F-05 / G-12 | 再計算 | 出荷した系列から Python が同じ傾きを出す |
| T-032 | F-05 | **陽性対照** | 平坦な系列で傾き 0、既知の傾きを持つ系列で厳密に一致 |
| T-033 | G-13 | 捨てた判断が実装に効いている | 港と海域の対応表を配信物に出していない |
| T-034 | G-15 | 実ブラウザの図検品 | 散布図の全要素が描画領域内(**陽性対照**: 枠を狭めれば溢れを検出する) |
| T-035 | N-05 | フッタ 5 項目の**行き先** | 項目ごとに行き先を照合する。「どれかが github.com を向いている」では足りない —— MIT License の行き先も github.com なので、GitHub 項目が化けても通る(HC-098)。App Menu は `app-menu-amber.vercel.app` であり、`app-menu.vercel.app` は**別人のアプリ**である |
| T-036 | G-18 | 記号欄の同形異字 | 記号欄の値は ○(U+25CB)と ◎(U+25CE)だけ。**陽性対照**: `fold_mark` が U+3007 を畳み、◎ は畳まず、未知の記号で例外 |
| T-037 | G-17 / G-08 | 学習の特徴 | 禁止特徴(管理者区分・座標・都道府県)を使わない。施設延長の無い港を 0 で埋めて学習表に入れない |
| T-038 | G-10 | ベースライン比較 | 多数派・規模だけの LR・全特徴 LR・勾配ブースティング・MLP を同じ交差検証(5 分割 × 5 反復以上)で比べ、全モデルが 5 種別の再現率を持つ |
| T-039 | G-16 / G-11 | ゲートの再計算 | 報告に書かれた判定が、報告の数から再計算して一致する。閾値は事前登録の 0.20。MLP は最良の非 NN を 1SE 超えたときだけ採用 |
| T-041 | G-10 / G-08 | 港ごとの AI 欄 | 全 2,768 港の詳細に AI 欄があり、学習表にある港は分割外予測と一致する推定値、無い港は「0 で埋めない」理由。両方の枝が実際に通る。公開した報告は canonical と同一 |
| T-042 | G-11 | 初期化乱数の頑健性 | 乱数を替えた macro-F1 から、線・線を越えた乱数・全乱数で成り立つかを再計算して一致。**経路の照合**: 出荷時の乱数での値が本体の mlp_full と一致(設定を書き写していない証拠) |
| T-043 | G-21 | 利用条件の区分表 | コード内の区分表が、保存した A40 ページの記載(再配信可・条件付き・提供不可の一覧)と**集合として一致**する(**非循環**: 区分表は手で写し、照合相手はページから機械で抜く)。漁港のある 40 都道府県すべてに区分がある |
| T-044 | G-21 / AC-012 | 出してはならない県 | リンクだけ・提供不可・データ無しの県の港は、配信物に区分の値を一つも持たない。**陽性対照**: 京都府の港に値を入れた配信物を検査が落とす |
| T-045 | G-19 | 内外を出さない | 配信物の津波の欄に「区域内/区域外」を表す鍵が無い |
| T-046 | G-20 | 半径の単調性 | 出す県の座標つきの港すべてで、半径は 100 / 200 / 500 m の 3 つ、最大区分の順位は半径とともに下がらない。**陽性対照**: 500 m の区分を 100 m より浅くした行を検査が落とす |
| T-047 | G-22 | 但し書き | 津波の配信メタに但し書きがあり、「安全」の語を含む区分ラベルが無い。実ブラウザで津波の表示に但し書きが出る |
| T-048 | G-20 | 区分ラベルの読み取り | 実測した書式 14 例(改行入り・空白入り・波ダッシュ無しを実物の字面どおり)を浸水深の区間に読み、同じ区間の別表記を一つの表示ラベルにそろえる。**陽性対照**: 読めない書式(文字だけ・上下の逆転・幅 0)は例外。並びは下限、次に上限。期待値の出所は全 51 ファイルの属性表の実測(`scripts/probe_a40_labels.py`: 60 種すべて読めて、どのファイルでも区間が重ならない) |
| T-049 | G-20 | 年度違いの刻みが混ざった県の最大 | 同じ県で区分が別の区分に**真に含まれる**(下限が大きく上限が小さい)組が無いことを、積んである canonical のメタ(`labels_by_prefecture`)で全県について確かめる(中間結果 data/interim/ は積まないので、そこを読むと新しい clone で skip して緑のまま何も見ない)。区分の集合が空でない県は、港の欄で区分の出ている県と一致する(方針の一覧とは比べない: 岐阜県は出す方針だが漁港が無い)。組があると「下限、次に上限」の最大が浅い上限を出すので、構築もその場で止める。**陽性対照**: `1〜5m` と `2〜3m` は組として拾い、実物の青森・福島の組(境界の共有・部分的な重なり)は拾わない |
| T-040 | G-10 | 事前登録と閉形式 | 期待表 E1〜E5 が揃う。多数派ベースラインの macro-F1 が、クラス分布から**閉じた式** 2p/(1+p)/5 で再計算して一致する(**非循環**: 学習器を通さない) |
