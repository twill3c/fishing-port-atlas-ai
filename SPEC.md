# SPEC.md — fishing-port-atlas-ai

<!-- scaffold template v1.30.0 から展開(2026-09-08)。以後このファイルはプロジェクトが育てる -->

Fishing Port Atlas AI — 日本全国の指定漁港を、公開データだけで地図・時系列・機械学習から眺める。

原典: `Fishing_Port_Atlas_AI_V1.0_Complete_Implementation_Spec.md`(以下「実装仕様書」)。
本 SPEC は実装仕様書を**実測で上書きした結果**を正本とする。上書きした箇所は §7 に列挙する。

## 1. 目的

全国 2,768 の指定漁港を基本地理単位として、水産庁・国土数値情報・気象庁の公開情報を
出典・粒度・不確実性を保ったまま一つの探索環境に統合する。
「地図に漁港を並べること」ではなく、**漁港 × 種別 × 海面水温 × 時系列 × AI** を
一つの画面で行き来できることに価値を置く。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 全国 2,768 港の漁港マスタを公開データから再現できる | must |
| F-02 | 座標を持つ港を地図に描き、種別で塗り分ける | must |
| F-03 | 種別フィルタ・漁港検索(港名/読み/都道府県/市町村)が動く | must |
| F-04 | 港をクリックすると詳細 Drawer が開き、基本情報と出典が読める | must |
| F-05 | 気象庁 沿岸海面水温(1982-)を海域単位で取得し、海域ごとの長期変化を読める形で出す | must |
| F-06 | Layer Manager でレイヤを独立に ON/OFF できる | must |
| F-07 | 年スライダーで対象年を変え、データが無い年は「無い」と明示する | must |
| F-08 | AI: 漁港の特徴ベクトルからクラスタと類似漁港を出す(ベースライン比較付き) | must |
| F-09 | データ充足率・集計粒度・出典を画面から辿れる | must |
| F-10 | 座標が無い港も一覧・検索から到達でき、「座標なし」と明示される | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 月額固定費 0 円で運用できる(外部 DB・cron 課金なし) | 構成の目視 + Vercel 設定 |
| N-02 | API キーがクライアントバンドルに現れない | ビルド成果物の走査テスト |
| N-03 | Vercel のビルド中に Python 学習・ETL を実行しない | `npm run build` のみで完結 |
| N-04 | 初期ロードは manifest と ports.min.json のみ | 生成物のサイズ実測 |
| N-05 | 再配布条件が確認できないデータを `public/` に出さない | ライセンス台帳と出力の突合テスト |

## 4. 品質基準(品質ゲート)

| ID | ゲート | 判定 |
|---|---|---|
| G-01 | 明細 PDF から作った漁港数が総括表の 2,768 と一致する | 一致必須 |
| G-02 | 種別内訳が総括表と一致する(1:2,031 / 2:524 / 3:101 / 特3:13 / 4:99) | 一致必須 |
| G-03 | 都道府県別の港数・種別内訳・管理者内訳が都道府県別表と一致する(40 県) | 一致必須 |
| G-04 | 特定第３種の集合が総括表脚注の 13 港名と集合として一致する | 一致必須 |
| G-05 | 漁港番号が全国で一意である | 一致必須 |
| G-06 | 座標を持つ港の割合が 90% 以上(目標 99%)。実測 98.30%(2,721/2,768、2026-09-08) | 90% 以上 |
| G-07 | すべての座標が日本の範囲(緯度 20–46 / 経度 122–154)に入る | 違反 0 件 |
| G-08 | 欠損を 0 で埋めない。値が無い欄は null + status で区別する | 違反 0 件 |
| G-09 | 日本語本文に別字種・制御文字が混入していない(`harness/text_hygiene.py`) | 違反 0 件 |
| G-10 | AI の各出力にモデル・学習期間・ベースライン比較が併記される | 全港の AI 欄と /ai/ に併記(T-038・T-041、loop_004) |
| G-11 | 深層学習モデルは、同じ指標でベースラインに勝ったときだけ本番採用する | **MLP を出さない**(loop_006 の学習し直し: MLP 0.458 < 最良の非 NN ロジスティック回帰の線 0.4725。初期化乱数 5 通りとも線を越えず、出荷はロジスティック回帰、T-039・T-042)。loop_004 は 0 を値として含む入力で MLP を採用していた |
| G-12 | 二実装照合: 海域の傾きを Python と TypeScript が独立に計算し、**中間量まで**一致する | 全 107 海域で一致 |
| G-13 | 港と海域の対応は、裏づけのある規則で決まるときだけ出す | 出荷しない(§7 参照) |
| G-14 | 長さの違う系列を同じ表で比べない(1982- と 2016- を混ぜない) | 既定で分離 |
| G-15 | 図の要素が描画領域(viewBox)からはみ出していない | 違反 0 件 |
| G-16 | 「種別は施設の規模で読める」と画面に書くのは、**規模だけ**(係留・外郭施設延長)のロジスティック回帰が、多数派ベースラインの macro-F1 を **0.20 以上**上回るときに限る(事前登録 2026-09-14。実測後に動かさない) | **通過**(loop_006 の学習し直し: 差 0.233。施設延長だけ 0.400 / 多数派 0.168、2,533 港、T-039)。loop_004 は 0 を値として含む入力で不通過(差 0.128)だった。閾値は動かしていない |
| G-17 | 学習の特徴に、**種別から法令上導かれる値**(漁港管理者の区分)と、地域を覚えられる値(座標・都道府県)を入れない | 違反 0(T-037) |
| G-18 | 記号欄の同形異字(U+3007 と U+25CB など)を畳んでから集計・学習する | 記号欄は ○ ◎ の 2 種のみ(T-036) |
| G-19 | 津波浸水想定について、**漁港の代表点が区域の内か外か**を出さない(福井県で代表点の 42/44 が区域外・中央値 47 m。偽の「区域外」になる) | 配信物に内外の鍵 0(T-045)・実ブラウザで「区域外」の語 0 |
| G-20 | 半径を一つに決めない。**半径 100 / 200 / 500 m** の最大浸水深区分を並べて出す。半径が大きいほど最大区分は下がらない(単調)ことをテストで固定する | 値のある 2,379 港で単調性の違反 **0**(T-046)。100→200 m で 704 港(29.6%)、200→500 m で 574 港(24.1%)の区分が変わる。真に含まれる区分の組 0(T-049) |
| G-21 | 再配布してよい都道府県だけを出す(AC-012)。**出す**: 再配信可 32 + CC BY 4.0 の規約を確かめた宮城・三重・島根・大分 + 解説を確認した千葉 = 37。**リンクだけ**: 京都(商用利用は要連絡)・長崎(再配信は事前連絡)。**提供不可**: 香川。**データ無し**: 滋賀。リンクだけ・提供不可の県の港に区分の値を配信物へ出さない | 出す 37 都道府県 2,401 港(値 2,379・座標が無い 22)。リンクだけ 255 港・提供不可 92 港・データ無し 20 港に値 **0**(T-043・T-044、実ブラウザで京都府の港に表が出ない) |
| G-23 | 国土数値情報 C09 の係留施設延長・外郭施設延長の **0 は欠測の符号**として扱い、学習表に値として入れない(loop_006 の実測: 八戸・釜石・舞鶴・油津が 0、青森 46.2%・岩手 37.2% に固まり、正の値は最小 3 m の整数で 0 と連続しない) | 学習表 **2,533 港**・0 の行 0・八戸は学習表に無い(T-050)。0 と 3 m のあいだの値が来たら止める |
| G-24 | C09 の施設延長を「施設の規模」と呼ばない。メタデータのとおり**普通交付税の算定基準に基づく数値で、実際の施設延長とは異なる**と、AI の報告・/ai/・港の AI タブに添える | 報告の但し書きがメタデータから機械で抜いた一文と一致・問いに「規模」なし(T-051)。実ブラウザで /ai/ と AI タブに但し書き |
| G-25 | 類似漁港(F-08)は、出荷する近傍(標準化した 12 特徴・ユークリッド距離の上位 10)の**公式の種別の一致率(種別ごとの平均、macro)**が、無作為に選んだ港の閉形式の期待値を **0.10 以上**上回るときだけ出す。種別は特徴に入れていないので外部の手がかりになる(事前登録 2026-09-15。実測後に動かさない) | **通過**(差 0.156。12 特徴 0.356 / 施設延長だけ 0.333 / 無作為 0.200、対象 2,533 港) |
| G-26 | 類似漁港は「何を似ているとみなすか」で答えが変わることを隠さない。出荷する近傍と**施設延長 2 特徴だけの近傍**の上位 10 の重なり(平均)を報告に載せ、**0.5 未満なら**画面に「特徴の選び方で似た港は大きく変わる」と、使った特徴の一覧を出す | 重なり **0.076** → 注意書きと特徴の一覧を AI タブと /ai/ に出す |
| G-27 | 二実装照合: 類似漁港の上位 10 を Python と TypeScript が独立に計算し、**全港で港番号と順序が一致**・距離が 1e-9 以内。同距離は**港番号の昇順**で並べる(規則を揃えないと同点で食い違う) | **全 2,533 港で一致**(T-054。同距離は距離を 1e-9 の格子に丸めた整数で判定。同点を降順にした変異体は食い違いを出す) |
| G-28 | クラスタ(F-08)は、ある k(3〜8)で次の**すべて**を満たすときだけ出す。(a) silhouette が列ごとに並べ替えた帰無を 0.05 以上上回る (b) 初期化の乱数 3 通りとの ARI の最小が 0.80 以上 (c) 80% 部分標本 5 回との ARI の最小が 0.80 以上 (d) 分割が単一の二値特徴で切った 2 群と ARI 0.80 未満(一つの印で切れているだけの群を「型」と呼ばない)。満たす k が複数なら (a) の差が最大の k。**満たさなければクラスタは出さず、測った数を /ai/ に書く**(事前登録 2026-09-15) | 規則上は **k=3 で通過**。ただし群は有人国境離島 × 海岸保全区域◎で切った分割と ARI **0.980**(事後の測定)で、規則 (d) の穴(§7.5)。群を港の型と呼ばず、港の AI タブには出さず、/ai/ に中身と穴を併記 |
| G-29 | CI(GitHub Actions)を置いたら、**最初の実行を完了まで見届けて緑を確かめ、run の ID を残す**。置いただけで「CI 済」としない(HC-270) | **最初の実行 run 34964180342(commit 729a376)で web・python・loop-verify とも成功**を `gh run watch --exit-status` で見届けた(T-057、2026-09-15) |
| G-30 | 積まない生データ(`data/raw`)に依存する検査は、無い環境では**理由つきで skip** し、在るのに合わなければ失敗する。CI では skip した検査の集合を**宣言した一覧**(`tests/ci_expected_skips.txt`)と照合し、一覧に無い skip が出たら失敗にする(skip は緑と見分けが付かないため) | 新しい clone(生データ・out なし)で **80 合格・7 skip・照合一致**(T-056)。導入前は C09 を読む 5 件が例外で落ちていた。照合の陽性対照: 一覧と一致で終了コード 0、宣言に無い skip で 1 |
| G-22 | 津波の表示には必ず但し書きを添え、「この半径に区域が無い」を安全と読める言葉で出さない(実装仕様書 §10.2) | 全港の防災タブに但し書き。「安全」は否定の句の中だけ(T-047、実ブラウザで出す県・リンクだけの県とも但し書きが出る) |

## 5. データ源(実測済み・2026-09-08)

| ID | データ | 取得 | 状態 |
|---|---|---|---|
| DS-001 | 水産庁 漁港一覧(総括表 + 都道府県別集計 + 都道府県別明細 40 県) | HTTP / PDF 42 本 | 取得済み |
| DS-002 | 国土数値情報 C09 漁港データ(点 2,931 / 区域線 3,292) | HTTP / Shapefile | 取得済み |
| DS-003 | 気象庁 日本沿岸域 海面水温(海域 107・日別値 1,684,291 日) | HTTP / TXT | 取得済み |
| DS-004 | 国土地理院 淡色地図タイル | 地図配信 | 背景に使用 |

### 使えなかった源(実測)

- **海しる API**(実装仕様書 §7): `MSIL_API_KEY` が必要。キー無しでは 404。
  仕様書が海しるに割り当てていた「漁港の点」「漁港区域」は **DS-002 が両方持つ**ので置換した。
- **e-Stat API**(実装仕様書 §8): `ESTAT_APP_ID` が必要。キー無しでは
  `STATUS:100 認証に失敗しました`。漁業センサス・海面漁業生産統計は V1.0 では**取得しない**。
  対応する画面の欄は「未取得」として空欄で出す(0 で埋めない)。
- **国土数値情報 A40 津波浸水想定**(実装仕様書 §10): 都道府県ごとに再配布条件が異なるため、
  ライセンス台帳を整備するまで取得しない(loop_005 以降)。

## 6. Canonical Data Model

### 6.1 port(主キー: `port_no`)

水産庁明細 PDF と国土数値情報 C09 が共有する**公式の 7 桁漁港番号**。
実装仕様書 §13.1 の `JP-FP-{pref_code}-{sequence}` は採用しない(§7 参照)。

```
port_no name_ja name_kana port_class pref_code prefecture municipality
administrator_name administrator_type fishery_coop designation_date area_change_date
island_peninsula inhabited_border_island coast_conservation port_regulation_law
visitor_berth subdistrict remarks lat lon geometry_method geometry_source source_pdf
```

`port_class`: `1` / `2` / `3` / `special_3` / `4`
`geometry_method`: `code_name_pref` / `code_pref` / `name_pref` / `boundary_centroid` / null

### 6.2 座標の突合段階と実測値(2026-09-08)

| 段階 | 条件 | 件数 |
|---|---|---:|
| `code_name_pref` | 漁港番号・港名・都道府県が一致 | 2,583 |
| `code_pref` | 漁港番号と都道府県が一致(港名は 2006 年以降に変わった) | 63 |
| `name_pref` | 番号は不一致だが同一県に同名の港が 1 つだけ | 51 |
| `boundary_centroid` | 漁港区域線の重心が県の外接矩形 +0.5° に入る | 24 |
| 座標なし | 上のいずれにも当たらない | 47 |

座標なし 47 港のうち 20 港は滋賀県である。C09(平成18年度)は滋賀県の漁港を 1 件も含まない(実測)。

## 7. 実装仕様書からの上書き(実測が先、仕様が後)

| 実装仕様書 | 実測でどうなったか |
|---|---|
| §7 海しる API を漁港の点・区域の主源とする | API キー必須。**国土数値情報 C09 で置換**(点 2,931・区域線 3,292 を無償・キー不要で取得) |
| §8 e-Stat API で漁業センサス・生産統計 | `ESTAT_APP_ID` 必須で認証失敗。V1.0 のスコープから外す |
| §13.1 主キーは `JP-FP-{pref}-{seq}` | 公式の 7 桁**漁港番号**が両源に存在。採番し直すと出所への遡行が切れるので採用しない |
| §17.3 スコア式 Identity Resolution(0.55×名称類似 ほか) | 番号が共通キーなので類似度合成は不要。**段階つき突合**(§6.2)に置き換え、各段階の件数を実測で公開する |
| §46 Q001 port count `expected = 2768` を定数で持つ | 定数で持たず、**総括表 PDF から毎回読む**。数がずれたら「公式が動いた」と分かる |
| §1.1 種別内訳の表 | 総括表の実測と一致(2,031/524/101/13/99)。ただし**都道府県別表の「第３種」は特定第３種を含む 114** である点は仕様書に無い |
| §18.2 漁港点を JMA 沿岸海域 polygon に spatial join する | **ポリゴンが配布されていない**。配られているのは海域番号と海域名の対応表だけ。§7.1 参照 |
| §9.1 area137 = 茨城県北部沿岸 | 一致(対応表で確認)。ただし海域は 107 あり、仕様書の一覧には無い |

### 7.1 港と海域の対応を出荷しないと決めた経緯(実測 2026-09-09)

海域名から場所を引く規則を書いて全 2,768 港に当て、次を測った。

| 内訳 | 件数 |
|---|---:|
| ちょうど 1 海域に当たった | 645(23.30%) |
| 2 つ以上に当たった | 721 |
| 1 つも当たらなかった | 1,402 |

107 海域のうち**都道府県名を含むのは 29** で、残りは振興局名・灘や湾の名前・島名で呼ばれている。
曖昧なものを「候補の列挙」に緩めれば 49.10% まで届くが、候補を目で見たら偽の一致だった ——
**青森県深浦町(日本海側)が、郡名「西津軽郡」を通って「津軽海峡」に当たる**。

規則は数を増やせるが、正しさは増えない。画面に出す海域名は
「この港の海域はここである」という主張なので、裏づけの無い主張は出さない(G-13)。
捨てた実装は `data-pipeline/integrate/assign_sst_area.py` に、測った数と一緒に残してある。

### 7.2 海面水温で測ったこと(実測 2026-09-09)

- 107 海域中 **102 海域が 1982 年から**取れる。残り 5 海域(大阪湾・播磨灘・備讃瀬戸・
  備後灘・燧灘・安芸灘・伊予灘・周防灘)は 2016 年からの速報値のみで平年値も無い。
  これは気象庁の説明ページが名指ししている 5 海域と**完全に一致した**(T-027)
- 長期系列 102 のうち **100 海域で傾きが正**、うち **92 海域が p<0.05**。
  傾きの中央値は **+0.264 ℃/10年**
- **冷たい海ほど速く上がっている。** 平年水温(緯度の代理)と傾きの相関は
  Pearson r = −0.435 / Spearman ρ = −0.565(p = 5.9e-10、n=102)。
  平年水温が低い四分の一の中央値 +0.358、高い四分の一 +0.184 ℃/10年
- 長期の傾きは**再解析値だけ**で出ている。速報値が入るのは取得時点の当年と上記 5 海域のみで、
  当年は有効日数 330 日の条件で自動的に外れる

### 7.3 漁港港勢を開いて、AI の問いを立て直した経緯(実測 2026-09-14)

- **漁港港勢(水産庁)は港別の表を持たない。**「漁港港勢の概要」(令和5年・22 頁)は
  漁港種類別の全国集計だけで、陸揚量の表は年が 1 セルに縦積みされていて表抽出できない。
  利用上の注意も港別の公開に触れていない。→ 港別の水揚げは V1.0 では取れない
- 実装仕様書 §22–29 の AI(魚種構成・水揚げ時系列の Autoencoder・予測)は、
  入力となる水揚げ・魚種・センサスが無いので**成り立たない**
- **(loop_006 で訂正)** C09 のメタデータ `KS-META-C09-06-g.xml`(Shift_JIS)は、この二つの属性を
  「普通交付税算定基準(普通交付税に関する省令第5条)に基づく数値であり、実際の施設延長数値とは異なる」と書いている。
  loop_004 はメタデータを読まずに「施設の規模」と呼んだ。以後は**交付税の算定に使う施設延長**と呼び、規模とは呼ばない(G-24)
- **(loop_006 で訂正)** 施設延長の **0 は欠測の符号**である(G-23)。C09 全 2,931 行で係留 0 が 186・外郭 0 が 151・両方 0 が 127。
  八戸(特定第3種)・釜石・舞鶴・油津が両方 0、0 は青森 42/91・岩手 42/113 に固まり(両方 0 の 127 のうち上位 5 県で 102)、
  正の値は最小 3 m の整数で 0 と連続しない(`scripts/probe_c09_zeros.py`)。loop_004 は 0 を値として学習していた
- 手元にある港別の量は、国土数値情報 C09(平成18年度)の**外郭施設延長・係留施設延長**
  (2,697/2,768 港)。係留施設延長の中央値は 第1種 258 / 第2種 867 / 第3種 1,712 /
  特定第3種 4,093 / 第4種 922 m で、種別との Spearman ρ=0.576(n=2,602、第4種を除く)
- そこで AI の問いを「**公式の種別は、施設の規模で読めるのか**」に置き換える。
  種別は法令上「利用範囲」で定義されており、規模ではない。読めるなら種別はほぼ規模であり、
  読めないなら種別は規模とは別の何かで決まっている —— **どちらの答えでも画面に出す**

学習の設計:

- 特徴は二組。**規模だけ**(log1p 係留施設延長・log1p 外郭施設延長)と、
  **規模+地域を覚えない属性**(離島・半島・国境離島・港則法適用・ビジター受入・分区数・海岸保全区域・指定年)
- **入れないもの(G-17)**: 漁港管理者の区分 —— 第3・第4種はほぼ都道府県が管理するので、
  種別の**帰結**であって手がかりではない。座標・都道府県 —— 地域を覚えて当ててしまう
- 評価は層化 5 分割 × 5 反復の交差検証、指標は macro-F1
  (特定第3種は 13 港しかないので、正解率では見えなくなる)
- ニューラルネットは **scikit-learn の MLP** で比べる。実装仕様書は PyTorch を指定しているが、
  表形式・2,697 行・特徴 10 個前後では PyTorch に利点が無く、venv にも入っていない

事前登録した予想(HC-064。**実測後に文面を変えない**。外れたら外れたと書く):

| ID | 予想 | 根拠 |
|---|---|---|
| E1 | 規模だけのロジスティック回帰は、多数派ベースラインの macro-F1 を上回る | 係留施設延長と種別が単調 |
| E2 | 第4種の再現率は、規模だけより属性を足したほうが高い | 第4種は辺地で定義され、規模は第2種並み |
| E3 | 特定第3種は第3種と取り違えられやすい | 特定第3種は政令指定で、規模は第3種の上側に連続している |
| E4 | MLP は勾配ブースティングを 1 標準誤差を超えて上回らない | 表形式で特徴が少ない |
| E5 | 管理者区分だけを特徴にすると、第3・第4種の再現率が規模だけより高い | 法令上の帰結だから。G-17 で外す理由の陽性対照 |

実測の結果(2026-09-14。`data/canonical/ai_class_report.json`、対象 2,697 港。
値は再計算しても同じだった —— 分割・初期化の乱数とも固定):

**(loop_006 で学習し直した結果が正本。施設延長 0 を欠測として外した 2,533 港。2026-09-15)**

| モデル | macro-F1 | 第1種 | 第2種 | 第3種 | 特定第3種 | 第4種 |
|---|---:|---:|---:|---:|---:|---:|
| 多数派 | 0.168 | 1.00 | 0.00 | 0.00 | 0.00 | 0.00 |
| ロジスティック回帰 施設延長だけ | 0.400 ± 0.002 | 0.76 | 0.31 | 0.60 | 0.73 | 0.28 |
| **ロジスティック回帰 施設延長+属性(出荷)** | **0.468 ± 0.004** | 0.73 | 0.53 | 0.63 | 0.60 | 0.47 |
| 勾配ブースティング | 0.421 ± 0.012 | 0.86 | 0.52 | 0.37 | 0.15 | 0.18 |
| MLP | 0.458 ± 0.004 | 0.74 | 0.56 | 0.61 | 0.47 | 0.41 |
| 管理者区分だけ(陽性対照・出荷しない) | 0.217 ± 0.001 | 0.85 | 0.00 | 0.00 | 0.07 | 0.88 |

- **G-16 通過(差 0.233)。答えが loop_004 と逆になった。** 施設延長だけのロジスティック回帰は多数派を 0.233 上回り、
  事前登録の 0.20 を越えた。loop_004 の「読めない」(差 0.128)は、欠測の印 0 を値として学習した入力での結果だった。
  外した 164 港の内訳は第1種 151・第2種 6・第3種 3・特定第3種 1(八戸)・第4種 3(`scripts/probe_c09_zeros.py`)。
  **差がなぜ 0.128 から 0.233 まで動いたかは、要因に分けて測っていない**(外した港の影響と、多数派の割合の変化を分けていない)
- **G-11: MLP を出さない。** 最良の非 NN はロジスティック回帰(施設延長+属性)0.468。MLP 0.458 は線 0.4725 に届かず、
  初期化の乱数 5 通り(0.4396〜0.4649)でも一度も越えなかった。出荷はロジスティック回帰
- E1 成立 / E2 成立(第4種 0.276 → 0.465)/ E3 成立(特定第3種の誤り 24 件中 22 件が第3種へ)/
  **E4 不成立**(MLP 0.458 が勾配ブースティング 0.421 ± 0.012 を上回った)/
  **E5 不成立**(管理者区分だけで第4種 0.878 だが第3種 0.000)
- 閾値・予想の文面・手順は loop_004 の事前登録のまま動かしていない。変わったのは入力(0 を欠測にした)だけ

loop_004 の結果(0 を値として含む 2,697 港。**欠陥のある入力による記録として残す**):

| モデル | macro-F1 | 第1種 | 第2種 | 第3種 | 特定第3種 | 第4種 |
|---|---:|---:|---:|---:|---:|---:|
| 多数派 | 0.169 | 1.00 | 0.00 | 0.00 | 0.00 | 0.00 |
| ロジスティック回帰 規模だけ | 0.297 ± 0.002 | 0.62 | 0.27 | 0.44 | 0.66 | 0.33 |
| ロジスティック回帰 規模+属性 | 0.407 ± 0.001 | 0.67 | 0.51 | 0.51 | 0.66 | 0.49 |
| 勾配ブースティング | 0.418 ± 0.010 | 0.88 | 0.51 | 0.37 | 0.15 | 0.14 |
| MLP | 0.454 ± 0.008 | 0.75 | 0.55 | 0.59 | 0.42 | 0.43 |
| 管理者区分だけ(陽性対照・出荷しない) | 0.220 ± 0.001 | 0.86 | 0.00 | 0.00 | 0.06 | 0.92 |

(種別の列は再現率。± は反復 5 回の標準誤差)

- **G-16 不通過。** 規模だけのロジスティック回帰は多数派を 0.128 しか上回らず、事前登録の 0.20 に届かない。
  **公式の種別は、施設の規模では読めない**
- **G-11: MLP を出す。** MLP 0.454 は、最良の非 NN(勾配ブースティング)の線 0.428 を越えた。
  初期化の乱数を 5 通り替えても 5 通りとも線を越えた(0.4354〜0.4540)が、
  最小の乱数では線との差が 0.007 で、**勝ちは僅差**である
- E1 成立 / E2 成立(第4種 0.331 → 0.493)/ E3 成立(特定第3種の誤り 22 件中 17 件が第3種へ)/
  **E4 不成立**(MLP が勾配ブースティングに勝った)/
  **E5 不成立**(管理者区分だけで第4種は 0.920 だが、第3種は 0.000)
- 勾配ブースティングは少数種別の再現率が低い(特定第3種 0.15・第4種 0.14)。
  ハイパーパラメータを調整していないので、E4 の外れは「MLP が本質的に強い」ではなく
  **「この設定どうしでは」**の結果として読む
- 収束警告は全モデルで 0 件
- 途中で**記号欄の同形異字**を見つけた(G-18)。海岸保全区域の 3 港で ○(U+25CB)の代わりに
  漢数字のゼロ 〇(U+3007)が使われていた。水産庁 PDF 側の揺れで、NFKC では畳まれない

### 7.4 津波浸水想定を、点の内外ではなく半径で出すと決めた経緯(実測 2026-09-14)

国土数値情報 A40(2024 年度ページ)の実物:

- 使用許諾は CC BY 4.0(一部制限)。配布のある 39 道府県のうち、**再配信可 32**・**条件付き 7**
  (宮城・千葉・三重・京都・島根・長崎・大分)。**提供不可は香川**。漁港があるのに配布が無いのは
  **香川 92 港・滋賀 20 港**
- 条件付き 7 の中身を読んだ: 宮城・島根・三重・大分は各県の規約が **CC BY 4.0**(出典の表示を求める)。
  千葉は「解説を確認すること」で、解説の留意事項(最大クラスの津波が悪条件下で発生した場合の想定、
  それより大きな津波が起きないというものではない、第二波以降に最大となる場所もある)は再配布の制限ではない。
  京都は商用利用に、長崎は再配信に**事前の連絡**が要るので、**リンクだけ**にする
- ダウンロードの URL はページに書かれていない。`https://nlftp.mlit.go.jp/ksj/gml/data/A40/A40-YY/A40-YY_PP_GML.zip`
  の規則で取れることを福井県で確かめた。年度違いの zip が複数ある県が 12 ある
- 再配信可 32 県の合計は最新年度だけで 1.14 GB、全年度で 1.57 GB

福井県(A40-23_18、面 19,846、漁港 44)で出し方を測った:

| 出し方 | 実測 | 判断 |
|---|---|---|
| 代表点が区域の内か外か(実装仕様書 §18.3) | 区域内 **2** / 区域外 **42**。区域外の点から区域まで中央値 47 m・最大 377 m | **出さない**(G-19)。代表点は水際にあり、陸を覆う区域の外に落ちる。偽の「区域外」になる |
| 半径 R 以内の最大浸水深区分を一つ | R=100→200 m で **17/44** 港、200→500 m で **11/44** 港の区分が変わる。「区域なし」は 8 → 1 → 0 港 | 一つの半径は選ばない |
| **半径 100 / 200 / 500 m の最大区分を並べる** | 上の変わり方そのものを見せられる | **採る**(G-20) |
| 区域の面を地図に重ねる | GeoJSON 相当 4.46 MB。3 m 以下の簡略化では面積が変わらず 3.05 MB、**10 m で面積差最大 21%・形のずれ最大 33%** | V1.0 では出さない。全国を形を崩さず配るにはベクトルタイルの生成が要る |

計算の決めごと(実測より前に固定する):

- 距離は**港ごとの局所平面**で測る(その港の緯度で経度方向を縮める)。県全体で一つの基準緯度を使うと、
  南北に長い県で半径がずれる
- 年度違いは、**新しい年度の面が覆う範囲を優先し、古い年度の面はそこから切り取った残りだけを使う**
  (A40 ページの「重複する箇所は最新年度を」)。こうして県ごとに面の集合を一つにしてから半径で引くので、
  **半径が大きいほど最大区分は下がらない**(G-20 の単調性)が構造上成り立つ
- 区分ラベルは**浸水深の区間(下限・上限)に読み**、表示は区間から作り直した一つの書き方にそろえる
  (`1m以上 ～ 3m未満`・`1.0m以上 ～ 3.0m未満`・`1～3m` はどれも `1〜3m`)。並びは下限、次に上限の順。
  読めない書式はその記録を読んだ時点で止める
  - 実測(2026-09-14、取得した 51 ファイルの属性表): 区分ラベル **60 種**・面 **5,303,091**。
    書式は `～0.3m` / `0.3～0.5m` / `5m～` / `1m以上 ～ 3m未満` / `20m以上` / `0.3m未満` /
    `～ 0.3m未満`(空白入り)/ `0.01m以上 0.3m未満`(波ダッシュ無し)/ `0.01m以上～0.3m未満` と末尾の改行(岡山 2016)。
    **60 種すべて読め、どのファイルでも区間が重ならない**(各ファイルの刻みは区間の分割として閉じている)
  - ただし**同じ県の年度違いでは刻みが混ざり、区間が重なる**(青森 2021 の `1〜3m` と 2016 の `1〜2m`・`2〜5m` など)。
    「下限、次に上限」の最大が最大浸水深の区間 [下限の最大, 上限の最大] と一致するのは、
    ある区分が別の区分に**真に含まれる**(下限が大きく上限が小さい)組が無いときだけなので、
    その組があれば構築を止め、全県の実物で組が無いことをテストで確かめる(T-049)
  - 刻みは都道府県・年度で違う(0.3/0.5/1/3/5/10/20 m、0.3/1/2/5/10/20 m、20〜50 m を持つ県など)。
    同じ県でも年度で刻みが変わる(福島 2023 と 2021)
  - 最初は福井県 1 県の語彙だけで規則を書き、北海道で止まった(PROC-SKIP、loop_005)
- 「この半径に区域が無い」は、安全という意味ではないと必ず添える(G-22)

全国の結果(2026-09-14、`scripts/summarize_tsunami.py` の出力から写した):

| 利用条件 | 都道府県 | 漁港 | 出したもの |
|---|---:|---:|---|
| 再配布してよい | 37 | 2,401 | 半径 3 通りの区分(座標が無い 22 港は理由だけ。岐阜県は漁港が無く構築しない) |
| リンクだけ(京都・長崎) | 2 | 255 | 理由と公式情報への案内 |
| 提供不可(香川) | 1 | 92 | 理由 |
| データ無し(滋賀) | 1 | 20 | 理由 |

| 半径 | 区域が掛からない港 | 次の半径で区分が変わった港 |
|---|---:|---:|
| 100 m | 662 / 2,379 | 704(29.6%) |
| 200 m | 252 / 2,379 | 574(24.1%) |
| 500 m | 69 / 2,379 | — |

- 構築した 36 都道府県の面は 5,271,790、港の 500 m 圏の外接矩形に掛かる面 515,346、
  年度の新しい面で切り取った古い面 89,952(うち丸ごと消えた 30,226)
- 全国の区分は 20 種。**全国の並び(rank_order)は表示の順で、県をまたいだ深さの比較には使わない**
  (たとえば徳島の `10m〜` は高知の `15〜20m` を真に含む)。最大は県の中でだけ取る
- 値に使われた年度は 2016〜2024 の 8 通り

### 7.5 類似漁港とクラスタ(F-08、loop_006)

判定の規則は G-25〜G-28 に実測前に書いた(2026-09-15)。対象は施設延長 0 を欠測として外した学習表 2,533 港・12 特徴。

最初のプローブ(0 を値として含む 2,697 港)で測ったこと —— 規則を書く前の観測:

- 特徴がまったく同じ行 54 行 14 組、標準化後の最近傍距離 0 が 50 港。0 を欠測にすると同じ行は **2 行**に減った
- k=4 の群の一つ(164 港)は**施設延長 0 の港そのもの**だった(HC-259 の縮退の形)。これを追って §7.3 の訂正に行き着いた

結果(`data/canonical/similar_report.json`、`data-pipeline/ml/similar.py`):

| 近傍の選び方 | 第1種 | 第2種 | 第3種 | 特定第3種 | 第4種 | macro |
|---|---:|---:|---:|---:|---:|---:|
| 出荷(12 特徴) | 0.827 | 0.397 | 0.265 | 0.150 | 0.139 | 0.356 |
| 施設延長 2 特徴だけ | 0.840 | 0.391 | 0.250 | 0.108 | 0.076 | 0.333 |
| 無作為(閉形式) | 0.721 | 0.200 | 0.038 | 0.004 | 0.036 | 0.200 |

(上位 10 近傍のうち、その港と同じ公式の種別の割合。種別ごとに平均し、macro はその 5 つの平均)

- **G-25 通過**(差 0.156)。似た港は AI タブに 10 港ずつ出す
- **G-26**: 施設延長だけの近傍との重なりは **0.076**。何を似ているとみなすかで答えがほぼ入れ替わるので、
  使った特徴の一覧と注意書きを一覧と同じ場所に出す
- **G-28 は k=3 だけが規則を満たした**(silhouette 0.209 / 帰無 0.148・乱数替え ARI 0.998・部分標本 ARI 0.995・
  単一の印との ARI 最大 0.667 = 海岸保全区域◎)
- **ところが群を言語化すると、群は印で切れていた。** 群 0(565 港)は海岸保全区域◎が 100%、群 1(1,593 港)は◎が 0%、
  群 2(375 港)は有人国境離島が 99%。係留施設延長の中央値(log1p)は 5.87〜6.19 でほとんど変わらない。
  **二つの印(有人国境離島 × 海岸保全区域◎)の組み合わせで切った分割との ARI は 0.980**(事後の測定。判定には使っていない)
- 規則 (d) は「一つの印で切った 2 群」しか見ておらず、**二つの印で切れる場合を覆っていなかった**(SPEC-GAP、loop_006)。
  規則を事後に書き換えて不通過にはしない。代わりに、群を港の「型」とは呼ばず、港の AI タブには出さない。
  /ai/ に規則の判定・群の中身・事後の ARI 0.980 と規則の穴を並べて出す

## 8. スコープ外(V1.0)

実装仕様書 §2.3 に加えて、V1.0 では次を扱わない。

- **港と海域の対応**(§7.1 の実測により、裏づけのある規則が書けないため)
- **港湾法の港湾**(例: 小名浜港。福島県のページで「重要港湾指定/昭和26年1月19日」「管理者/福島県」と確認、2026-09-17)。
  指定漁港ではないので載らない。検索で 0 件のときと出典ページ(`/data/#not-listed`)で理由を案内する(loop_008)
- 漁業センサス・海面漁業生産統計(e-Stat 認証が必要なため)
- 津波浸水想定の**区域の面を地図に重ねること**(§7.4 の実測: 形を崩さず全国を配るにはベクトルタイルの生成が要る)。
  港ごとの半径の区分は出す(G-19〜G-22)
- 主要漁港別上場水揚量(海しる API が必要なため)
- GNN・Transformer による予測(実装仕様書 §2.2 の実験扱いに従い、V1.0 では出さない)
- リアルタイム情報・避難判断・行政評価・漁業権の法的判断
