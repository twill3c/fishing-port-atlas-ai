import type { Metadata } from "next";

import { loadStats } from "@/lib/loadPublicData";

export const metadata: Metadata = {
  title: "方法 | Fishing Port Atlas AI",
};

export default function MethodologyPage() {
  const stats = loadStats();
  const coverage = (stats.built.with_coordinates / stats.built.total) * 100;

  return (
    <main className="article">
      <p>
        <a href="/">← 地図へ戻る</a>
      </p>
      <h1>設計図 — どう作り、何を確かめたか</h1>

      <h2>1. 何を測ったら作れると判断したか</h2>
      <p>
        実装仕様書は、漁港の位置を「海しる API」から、漁業統計を「e-Stat API」から取る前提だった。
        どちらも認証キーが要る。キー無しで実際に叩いたところ、海しるは 404、e-Stat は
        「認証に失敗しました」を返した。そこで仕様書の前提を実測で置き換えている。
      </p>
      <ul>
        <li>
          <strong>漁港の位置と区域</strong> — 国土数値情報「漁港データ」(C09)が、
          点 2,931 件と区域線 3,292 本を鍵なしで配っている。海しるの代わりになる
        </li>
        <li>
          <strong>漁港マスタ</strong> — 水産庁「漁港一覧」の都道府県別 PDF 40 本。
          港名・読み・種別・所在地・管理者・漁協・指定年月日まで載っている
        </li>
        <li>
          <strong>漁業統計</strong> — 代わりが無い。V1.0 では出さず、欄を「未取得」で残す
        </li>
      </ul>

      <h2>2. PDF をどう読んだか</h2>
      <p>
        明細 PDF は横罫の無い表である。表抽出にかけると、列ごとに全行を改行で連結した
        「1 行の表」が返り、疎な列(分区・海岸保全区域)は空セルが落ちて行と対応しなくなる。
        そこで表抽出は使わず、<strong>罫線の座標から列境界を取り、文字を 1 字ずつ列へ割り当てた</strong>。
      </p>
      <p>
        語単位ではうまくいかない。隣り合う列のあいだに空白が無いので、
        <code>1110050斜内</code> のように<strong>列をまたいだ 1 語</strong>ができる
        (秋田県の PDF で漁港番号が 1 件も取れなかった)。文字単位なら、各文字の x で列が一意に決まる。
      </p>
      <p>
        行高も一定ではなかった。北海道の「内路」は漁協が 2 つ(香深・船泊)で 2 行になり、
        漁港番号はその 2 行の<strong>あいだ</strong>に置かれている。等間隔でバンドを切ると
        番号が 2 つ入る行ができる。隣り合う番号行の中点を境界にして解決した。
      </p>

      <h2>3. 何を正解として突き合わせたか</h2>
      <p>
        明細から作った表を、明細を数えた値で検査しても何も分からない。
        <strong>明細とは別の 2 枚の文書</strong>を正解に使っている。
      </p>
      <ul>
        <li>
          <strong>総括表</strong>(sub81-256.pdf) — 種別ごとの全国計と、
          特定第3種 {stats.official.special_class_3_names.length} 港の港名
        </li>
        <li>
          <strong>都道府県別集計</strong>(sub81-257.pdf) — 都道府県 × 種別 × 管理者区分
        </li>
      </ul>
      <p>照合したのは 4 系統で、すべて一致した。</p>
      <ol>
        <li>総数 {stats.built.total.toLocaleString()} 港</li>
        <li>種別内訳(第1種 {stats.official.by_class["1"].toLocaleString()} ほか)</li>
        <li>都道府県 40 件それぞれの港数・種別内訳・管理者内訳</li>
        <li>特定第3種の港名の集合</li>
      </ol>
      <p>
        3 番目は、明細の「漁港管理者」欄の<strong>文字列</strong>から
        「都道府県が管理者か市町村か」を導いて突き合わせている。導出規則そのものを
        外部の数が検算している形になる。
      </p>

      <h2>4. 通説がひとつ壊れた</h2>
      <p>
        都道府県別集計の「第3種」列は <strong>114</strong> で、総括表の第3種 101 と合わない。
        実測すると 101 + 特定第3種 13 = 114 で、
        <strong>都道府県別表の第3種は特定第3種を含んでいる</strong>。
        実装仕様書の種別表にはこの注意が無い。照合を書くときに気づいた。
      </p>

      <h2>5. 座標をどこまで信じるか</h2>
      <p>
        マスタは令和8年、座標は平成18年である。番号が一致しても、20 年のあいだに
        統廃合・改称で別の港に振り替わっていることがある。実測すると、番号が一致した
        2,676 港のうち 68 港は港名が違った。そこで突合を段階に分け、
        港ごとに<strong>どの段階で座標が付いたか</strong>を残している(詳細画面の「座標」欄)。
      </p>
      <p>
        国土数値情報の側も内側で整合していない。<strong>漁港番号が重複する 30 コード</strong>があり、
        重複行では港名・管理者・指定年月日が別の港のものに置き換わっている
        (1410690「宇島」が福岡県豊前市と宮城県石巻市の 2 点を持つ)。
        重複コードは、市区町村コードの上 2 桁が都道府県と一致する候補が
        ちょうど 1 つのときだけ採っている。
      </p>
      <p>
        区域線から重心を取る最後の段階には、<strong>同じ県の既知座標の外接矩形</strong>という
        ガードを置いた。飾りではない —— 石川県「中島」(2410125)の区域線が
        35.611N/134.467E(石川県の外ではるか西)を指しており、このガードが実際に落とした。
      </p>
      <p>
        結果、座標が付いたのは {stats.built.with_coordinates.toLocaleString()} 港
        ({coverage.toFixed(2)}%)である。残りは<strong>座標なしと明示して</strong>一覧から辿れるようにした。
        うち 20 港は滋賀県で、国土数値情報の平成18年度版は滋賀県の漁港を 1 件も含まない。
      </p>

      <h2>6. 欠損を 0 で埋めない</h2>
      <p>
        値が無い欄は <code>null</code> にし、なぜ無いかを
        <code>observed</code> / <code>estimated</code> / <code>suppressed</code> /
        <code>not_available</code> / <code>not_applicable</code> /
        <code>missing_source</code> の語彙で区別している。
        画面では「データなし」「対象外」「未取得」と出し分ける。
        0 と書けば、読み手はそこに 0 があったと受け取る。
      </p>

      <h2>7. その後に足したもの</h2>
      <ul>
        <li>
          <a href="/ocean/">海の温度</a> —— 気象庁の沿岸海域 107 の海面水温を 1982 年から通しで測った。
          港と海域の対応は、海域名から引く規則が偽の一致を出したので付けていない
        </li>
        <li>
          <a href="/ai/">AI</a> —— 「公式の種別は、施設の規模で読めるのか」を、
          実測の前に決めた合格ラインと予想で測った。ニューラルネットは、
          同じ交差検証で最良の非ニューラルネットに勝ったときだけ出す。
          読めなかった結果も、外れた予想も、そのまま出している
        </li>
      </ul>
      <p className="hint">
        実装仕様書が想定した AI(魚種構成や水揚げ時系列の Autoencoder・予測)は、
        港別の水揚げ・魚種が公開されていないので作っていない。
      </p>
    </main>
  );
}
